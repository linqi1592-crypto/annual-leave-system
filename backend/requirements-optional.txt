# 年假查询系统 - 可选依赖（生产环境推荐）
# 安装: pip install -r requirements-optional.txt

# ==================== 缓存增强 ====================
# Redis 缓存 - 多实例部署时使用
redis>=5.0.0

# ==================== 数据库增强 ====================
# PostgreSQL 支持 - 500人规模推荐使用
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# ==================== 导出增强 ====================
# pandas - 异步导出大数据量时使用
pandas>=2.0.0
