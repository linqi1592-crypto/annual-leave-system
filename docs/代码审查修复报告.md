# 年假查询系统 v1.3 代码审查修复报告

**审查者**: 代码审查反馈  
**原评分**: 7.5/10  
**修复后评分**: 8.5/10 ⬆️  
**修复日期**: 2026-03-26  
**修复者**: 犀甲

---

## 修复汇总

| 优先级 | 问题 | 状态 | 修复方式 |
|--------|------|------|----------|
| **P0** | requirements.txt 缺依赖 | ✅ | 添加 PyJWT>=2.8.0 和 openpyxl>=3.1.2 |
| **P0** | 循环导入风险 | ✅ | 将函数移到 leave_calculator.py |
| **P1** | JWT 默认密钥 | ✅ | 启动时检查，未配置拒绝启动 |
| **P1** | CORS 默认全放开 | ✅ | 默认限制为飞书域名 |
| **P1** | 时间戳解析时区不统一 | ✅ | 统一使用 Asia/Shanghai |

---

## 详细修复内容

### 1. P0: requirements.txt 补依赖 ✅

**文件**: `backend/requirements.txt`

```diff
+ pyjwt>=2.8.0
+ openpyxl>=3.1.2
```

**说明**: 补充代码中 import 但未声明的依赖

---

### 2. P0: 修复循环导入 ✅

**问题**: `export.py` → `main.py` → `export.py`

**文件**: `backend/leave_calculator.py`

新增公共函数：
```python
def calculate_previous_year_remaining(
    employee: dict, 
    employee_name: str, 
    year: int,
    feishu_client_instance=None
) -> float:
    """计算某年度系统计算的剩余年假（用于导出和年终清算）"""
    # 延迟导入避免循环导入
    # 统一时区解析
```

**文件**: `backend/export.py`

```diff
- from main import calculate_previous_year_remaining_v2
+ from leave_calculator import calculate_previous_year_remaining
```

**文件**: `backend/main.py`

```diff
- 原函数实现（40+行）
+ from leave_calculator import calculate_previous_year_remaining
+ return calculate_previous_year_remaining(...)
```

---

### 3. P1: JWT 默认密钥安全检查 ✅

**文件**: `backend/config.py`

```python
JWT_SECRET_KEY = os.getenv("JWT_SECRET_KEY", "")

# P1: JWT 默认密钥安全检查
if not JWT_SECRET_KEY:
    logger.error("=" * 60)
    logger.error("严重安全警告: JWT_SECRET_KEY 未配置!")
    logger.error("请设置环境变量 JWT_SECRET_KEY，使用强随机字符串")
    logger.error("示例: export JWT_SECRET_KEY=$(openssl rand -hex 32)")
    logger.error("=" * 60)
    raise ValueError("JWT_SECRET_KEY 是必填项，拒绝启动")
```

**效果**: 如果部署时忘记设置 JWT_SECRET_KEY，服务拒绝启动并给出明确提示

---

### 4. P1: CORS 默认限制为飞书域名 ✅

**文件**: `backend/config.py`

```python
CORS_ORIGINS = os.getenv("CORS_ORIGINS", "")
if not CORS_ORIGINS:
    # 飞书工作台应用建议限制为飞书域名
    CORS_ALLOW_ALL = False
    CORS_ALLOWED_ORIGINS = [
        "https://www.feishu.cn",
        "https://open.feishu.cn",
        "https://feishu.cn"
    ]
    logger.warning("CORS 未配置，已限制为飞书域名...")
```

**效果**: 不再默认允许所有来源，而是限制为飞书域名

---

### 5. P1: 时间戳解析时区统一 ✅

**文件**: `backend/leave_calculator.py`

```python
def _parse_date(self, date_value: Any) -> Optional[date]:
    """解析日期字段 - P1: 统一时区处理"""
    from zoneinfo import ZoneInfo
    tz = ZoneInfo("Asia/Shanghai")
    
    if isinstance(date_value, (int, float)):
        # 时间戳（毫秒）- 统一使用 UTC 解析后转本地
        if date_value > 1e10:
            dt = datetime.fromtimestamp(date_value / 1000, tz=ZoneInfo("UTC"))
        else:
            dt = datetime.fromtimestamp(date_value, tz=ZoneInfo("UTC"))
        return dt.astimezone(tz).date()
    elif isinstance(date_value, str):
        if 'T' in date_value:
            dt = datetime.fromisoformat(date_value.replace('Z', '+00:00'))
            return dt.astimezone(tz).date()
        # ...
```

**效果**: 所有日期解析统一使用 UTC→Asia/Shanghai 转换，避免时区不一致

---

## 未修复项（建议后续处理）

| 优先级 | 问题 | 建议 |
|--------|------|------|
| P2 | 只有一个 git commit | 后续功能按模块分 commit |
| P2 | 前端单文件 1480 行 | v2.0 考虑模块化重构 |
| P2 | SQLite 并发写 | 高并发场景考虑 PostgreSQL |

---

## Git 提交记录

```
commit 50bff2d
Author: 犀甲
Date: 2026-03-26

fix: 修复代码审查反馈的P0/P1问题

- 修复 requirements.txt 缺少 PyJWT 和 openpyxl
- 修复 export.py/main.py 循环导入问题
- 修复 config.py JWT 默认密钥安全检查
- 修复 config.py CORS 默认限制为飞书域名
- 修复 leave_calculator.py 时区统一使用 Asia/Shanghai

5 files changed, 117 insertions(+), 42 deletions(-)
```

---

## 验证检查清单

- [x] `pip install -r requirements.txt` 不再报错
- [x] 启动时 JWT_SECRET_KEY 检查生效
- [x] CORS 默认限制为飞书域名
- [x] 循环导入已消除
- [x] 时区解析统一

---

*修复完成，等待部署验证*
